<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion des Événements{% endblock %}</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
</head>
<body>
    <header class="bg-blue-500 text-white py-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">
                <a href="{{ path('home') }}">Gestion des Événements</a>
            </h1>
            <nav>
                <a href="{{ path('home') }}" class="text-white hover:underline mx-2">Accueil</a>
                <a href="{{ path('event_list') }}" class="text-white hover:underline mx-2">Événements</a>
            </nav>
        </div>
    </header>

    <main class="container mx-auto my-8">
        {% block body %}{% endblock %}
    </main>

    <footer class="bg-gray-800 text-white py-4">
        <div class="container mx-auto text-center">
            <p>&copy; {{ "now"|date("Y") }} Gestion des Événements Sportifs. Tous droits réservés.</p>
        </div>
    </footer>
</body>
</html>